((() => {

  var Vuex = window.Vuex

  const Store = new Vuex.Store({
    state: {
      // Vuex state
    },

    mutations: {
      // Vuex mutations
    },

    actions: {
      // Vuex actions
    },
  })
  window.store = Store
}))()